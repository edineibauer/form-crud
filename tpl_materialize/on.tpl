<div class="switch">
    <label>
        {$off}
        <input type="checkbox" class="{$class}" id="{$column}" ng-model="{$ngmodel}">
        <span class="lever"></span>
        {$on}
    </label>
</div>